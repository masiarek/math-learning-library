# SAP

**Level:** reference · staging area

This folder holds SAP material written in the same house style as the mathematics chapters: one idea per folder, a `README.md` per idea, and program output that is generated by `tools/run_examples.py` rather than typed. It is staged here so that it can be moved into the SAP library as a unit; every link inside it is relative to its own folder, so the tree can be copied verbatim.

| Chapter | The question it answers |
|---|---|
| [Currencies in SAP](currency/README.md) | Which amount is in which currency, why, and where the rate came from |
